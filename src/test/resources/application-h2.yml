spring:
  datasource:
    url: jdbc:h2:tcp://localhost:${h2.port:9090}/mem:${h2.dbName:mydb};MODE=${h2.mode:MYSQL};DATABASE_TO_UPPER=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password: 123456

sync-session:
  timeout: 30m
  clazz: pers.clare.test.session.TokenSession
  ds:
    table-name: test_session
